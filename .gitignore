# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/

# Tooling. There is no build step; node_modules only appears if someone installs a local server.
node_modules/
dist/
.cache/
*.log
npm-debug.log*
package-lock.json

# Raw Claude Design downloads dropped here for a re-sync. The unbundled files are the repo; exports stay local.
_source-exports/
*.zip
AiXFreight Design System*.html
.render-check.json

# Generated by the design-system compiler. Commit them so consumers work without a build, but never hand-edit:
#   _ds_bundle.js
#   _ds_manifest.json
#   _adherence.oxlintrc.json
