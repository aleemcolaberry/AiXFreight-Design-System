Input — labelled text field. Labels are mono caps; errors turn border + label danger red.

```jsx
<Input label="Origin port" icon="mapPin" placeholder="Shenzhen, CN" />
<Input label="Weight" suffix="kg" error="Exceeds container limit" />
```