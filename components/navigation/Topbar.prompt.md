Topbar — 56px header. Primary action goes in `actions` (top-right, one orange button).

```jsx
<Topbar title="Shipments" actions={<Button size="sm" icon="plus">New quote</Button>} user={{name:'Maya Krishnan'}} notifications />
```