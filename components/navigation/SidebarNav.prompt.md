SidebarNav — the 240px navy app rail. Active item gets a 3px orange inset bar.

```jsx
<SidebarNav base="../.." value={page} onChange={setPage} items={[{value:'dash',label:'Dashboard',icon:'layoutDashboard'},{value:'ship',label:'Shipments',icon:'package',count:3}]} />
```