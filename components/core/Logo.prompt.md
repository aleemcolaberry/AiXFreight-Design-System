Logo — renders the real lockup/mark SVGs. Set `base` to the path of the design-system root from the page.

```jsx
<Logo variant="horizontal" height={36} base="../.." />
<Logo variant="mark" inverse height={28} base="../.." />
```