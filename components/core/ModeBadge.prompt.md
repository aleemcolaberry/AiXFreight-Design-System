ModeBadge — chain-link-shaped pill carrying the industry mode color (land asphalt, air cyan, ocean blue, eco teal, AI orange). Never reassign mode colors.

```jsx
<ModeBadge mode="ocean" />
<ModeBadge mode="air" inverse size="sm" />
```