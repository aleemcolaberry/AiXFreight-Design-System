IconButton — icon-only square button for toolbars and top bars; always give `label`.

```jsx
<IconButton icon="bell" label="Notifications" badge />
<IconButton icon="plus" label="New shipment" variant="filled" />
```