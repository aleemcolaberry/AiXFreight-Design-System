Tabs — `underline` (default, orange indicator) for page sections; `pill` for compact segmented switches.

```jsx
<Tabs items={[{value:'all',label:'All',count:24},{value:'exc',label:'Exceptions',count:3}]} value={tab} onChange={setTab} />
```